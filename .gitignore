# Python
__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.ipynb_checkpoints/

# Analysis outputs and cached structures - regenerate these, don't commit them
covalent_pocket_mapper_results/
results/
structures/
*.pdb
*.cif

# Mass spec data - too large for git, keep it outside the repo
*.mzid
*.mzid.gz
*.MGF
*.mgf
*.raw

# The bundled example is exempt from all of the above. It is small, synthetic,
# and the only way to try the tool without already having your own data.
!examples/
!examples/structures/
!examples/**

# macOS
.DS_Store
